backend:
  name: git-gateway
  branch: main

media_folder: "images"
public_folder: "/images"

collections:
  - name: "galeria"
    label: "Galería"
    folder: "images"
    create: true
    slug: "{{filename}}"
    fields:
      - { label: "Imagen", name: "image", widget: "image" }
